# Database URL for Prisma
# For SQLite (default - local file)
DATABASE_URL="file:./dev.db"

# For PostgreSQL (if you want to use a remote database)
# DATABASE_URL="postgresql://username:password@localhost:5432/finance_tracker"

# Next.js Configuration
NEXT_PUBLIC_APP_NAME="Personal Finance Tracker"

# Optional: Set a custom currency (default: USD)
# NEXT_PUBLIC_CURRENCY="USD"

# Optional: Set date format preferences
# NEXT_PUBLIC_DATE_FORMAT="MM/dd/yyyy"

# Development only
NODE_ENV="development"